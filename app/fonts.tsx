import { Baloo_2, Nunito } from "next/font/google";
export const baloo = Baloo_2({
  subsets: ["latin"],
  display: "swap",
});

export const nunito = Nunito({
  subsets: ["latin"],
  display: "swap",
});
